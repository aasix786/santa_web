PAYPAL_MODE=sandbox
PAYPAL_SANDBOX_API_USERNAME=sb-omcrk20543166_api1.business.example.com
PAYPAL_SANDBOX_API_PASSWORD=6EA3QNTMJSVM345Z
PAYPAL_SANDBOX_API_SECRET=AVxojh21GaEm-ayxn.nRWNuiTNRuAP5WmBqgmr6Bb0rjitno6ZEWpc86
PAYPAL_CURRENCY=USD
PAYPAL_SANDBOX_API_CERTIFICATE=

1)overly is not showing on mobile view on image page.
2)water mark and overly is not showing on mobile view  paypal page
3)it is little bit difficult to resize image of sanata on image page (website) more difficult on mobile view.


sb-tcisb2553041@personal.example.com
123456789

.........

 $paypal =$this->paypalPaymentCurl($payment_add, $receiver_id);
            if (@$paypal->error) return redirect()->back()->withErrors('some paypal error occured');
            if ($paypal->paymentExecStatus =='CREATED'){
                $payment = $this->paymentDetails($payment_add, $paypal->payKey);

                return \redirect('https://www.paypal.com/cgi-bin/webscr?cmd=_ap-payment&paykey='.$paypal->payKey);
            }
  private function paypalPaymentCurl($id, $receiver_id){
        $obj_rec = [
            'amount' =>$payment,
            'email' =>'sb-eiwgf2539879@personal.example.com'
        ];
        $obj_rec = (object)$obj_rec;
        $json  =[
            'actionType' =>'PAY',
            'currencyCode'=>'USD',
            'receiverList'=>[
                'receiver' =>[$obj_rec
                ]
            ],
            'returnUrl'=>'https://construck.world/accept-donation/'.$id.'/'.$receiver_id,
            'cancelUrl'=>'https://construck.world/remove-donation/'.$id.'/'.$receiver_id,
            'requestEnvelope'=>[
                'errorLanguage'=>'en_US',
                'detailLevel'=>'ReturnAll'
            ]
        ];

        $curl = curl_init();

        curl_setopt_array($curl, array(
            CURLOPT_URL => "https://svcs.paypal.com/AdaptivePayments/Pay",
            CURLOPT_RETURNTRANSFER => true,
            CURLOPT_ENCODING => "",
            CURLOPT_MAXREDIRS => 10,
            CURLOPT_TIMEOUT => 30000,
            CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
            CURLOPT_CUSTOMREQUEST => "POST",
            CURLOPT_POSTFIELDS => json_encode($json),
            CURLOPT_HTTPHEADER => array(
                "accept: */*",
                "X-PAYPAL-SECURITY-USERID: RIScomics_api1.gmail.com",
                "X-PAYPAL-SECURITY-PASSWORD: QHPQYLQ3YRGHA9WQ",
                "X-PAYPAL-SECURITY-SIGNATURE: A1uq45jvJNYhMxrB6DEnCE4aThlxAykmILOo5nP-hWXn.xhtNmEgD8Zb",
                "X-PAYPAL-REQUEST-DATA-FORMAT: JSON",
                "X-PAYPAL-RESPONSE-DATA-FORMAT: JSON",
                "X-PAYPAL-APPLICATION-ID: APP-80W284485P519543T",
                "Accept: application/json",
                "content-type: application/json",
            ),
        ));

        $response = curl_exec($curl);
        $err = curl_error($curl);

        curl_close($curl);

        if ($err) {
            return "cURL Error #:" . $err;
        } else {
            return json_decode($response);
        }

    }
    